## 1. 模型思考能力契约

- [x] 1.1 在后端定义模型思考能力、请求选择、有效选择和调整原因的类型与序列化 schema
- [x] 1.2 将现有 Provider/模型推理规则整理为共享能力矩阵，覆盖可切换、强制开启和不支持三类模型
- [x] 1.3 实现纯函数能力解析与选择规范化，验证深度、开关和能力版本并提供安全默认值
- [x] 1.4 为能力矩阵、未知模型、过期深度和强制开启规范化添加参数化单元测试

## 2. 能力 API 与运行请求

- [x] 2.1 增加批量模型思考能力解析 API，并确保响应不包含凭据或 Provider 私有数据
- [x] 2.2 将 CreateRun、ContinueRun 与批准继续请求中的模型配置改为有类型的 schema，并接受可选 `thinking` 对象
- [x] 2.3 拒绝未知模型配置字段和 Provider 原生推理参数，保留现有模型配置兼容性
- [x] 2.4 添加能力 API、有效请求、非法原生字段、过期版本和旧客户端请求的 API 测试

## 3. 运行时绑定与 Provider 映射

- [x] 3.1 在创建 Run 时规范化模型思考选择，并将 requested/effective/source/adjustments 写入 `model_policy`
- [x] 3.2 扩展 ModelClient 绑定接口，使 Run 的有效模型思考配置用于所有模型操作
- [x] 3.3 更新 OpenAI、Anthropic 与 Qwen 适配映射，支持能力声明中的模型专属深度、开关和有界 budget
- [x] 3.4 保留缺少显式选择时从 `reasoning_policy.reasoning_effort` 推导参数的旧客户端兼容路径
- [x] 3.5 在计划确认、工具审批和用户输入后继续运行时恢复原 Run 快照，防止编辑器偏好覆盖
- [x] 3.6 扩展 ModelInvocation usage metadata，记录实际适配器、来源、有效状态、深度和非敏感参数摘要
- [x] 3.7 添加运行时测试，验证 Agent 编排强度与模型思考深度独立组合、快速/可信模式不覆盖显式选择、续跑一致性及安全降级
- [x] 3.8 验证关闭或改变模型思考深度不会停止公开阶段与 reasoning-summary 事件，也不会泄漏隐藏思维链

## 4. 聊天框模型选择体验

- [x] 4.1 在前端 API 与类型层接入模型思考能力批量查询和运行请求 `thinking` 配置
- [x] 4.2 为已启用模型加载并缓存能力，在能力查询失败时采用不可操作的兼容回退状态
- [x] 4.3 在模型菜单的模型区增加思考开关、模型专属深度选择、强制开启和不支持说明
- [x] 4.4 将模型思考控制、可信策略和界面可审计过程区分，并更新模型选择按钮摘要、影响提示及策略帮助文案
- [x] 4.5 以 `provider:model` 为键版本化保存用户偏好，在回答模式切换时保持选择，并仅在模型或能力变化时恢复或规范化
- [x] 4.6 在创建、继续和批准后继续请求中提交正确的当前或 Run 快照模型思考配置
- [x] 4.7 补充中文与英文文案、键盘/辅助技术标签以及桌面和移动布局样式

## 5. 验证与发布

- [x] 5.1 增加前端测试，覆盖可选开关、关闭、强制开启、不支持、按模型恢复、模式切换保持选择、过期偏好和模式影响提示
- [x] 5.2 增加请求与过程集成测试，验证标准与可信模式都提交模型思考配置且不改变 Agent 策略或过程面板规则
- [x] 5.3 运行后端与前端相关测试、类型检查和构建，并修复回归
- [x] 5.4 使用可选思考、强制思考、不支持模型和旧客户端在快速/可信模式各执行端到端烟雾测试，核对 Run 快照、usage metadata 与过程面板
